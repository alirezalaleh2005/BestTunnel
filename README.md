
🛡️ راهنمای جامع ابزار BestTunnel (نسخه Ultimate)
این اسکریپت ابزاری قدرتمند برای ایجاد اتصال امن و پرسرعت بین دو سرور (معمولاً ایران و خارج) است که با استفاده از پروتکل‌های لایه هسته لینوکس، کمترین تاخیر (Ping) و بیشترین سرعت را برای شما فراهم می‌کند.
🛠️ آموزش نصب سریع
برای نصب کافیست دستور زیر را در ترمینال سرور خود کپی و اجرا کنید:
bash <(curl -Ls https://raw.githubusercontent.com/alirezalaleh2005/BestTunnel/main/install.sh)

نکته: پس از نصب، هر بار که بخواهید وارد منوی مدیریت شوید، فقط کافیست کلمه besttunnel را تایپ کنید.
📖 مراحل راه‌اندازی (قدم به قدم)
۱. آماده‌سازی سرورها
شما به دو سرور نیاز دارید:
 * سرور ایران: به عنوان مقصد اول کاربران.
 * سرور خارج: به عنوان خروجی اینترنت.
۲. اجرای اسکریپت در هر دو سرور
در هر دو سرور اسکریپت را نصب و اجرا کنید.
۳. تنظیم سرور ایران
 * در منو گزینه 1 (Setup Tunnel) را انتخاب کنید.
 * نقش سرور را 1 (IRAN Server) انتخاب کنید.
 * Remote IP: آی‌پی سرور خارج را وارد کنید.
 * Internal IP: یک رنج آی‌پی دلخواه (مثلاً 10.0.0) وارد کنید یا اینتر بزنید تا پیش‌فرض انتخاب شود.
۴. تنظیم سرور خارج
 * در منو گزینه 1 (Setup Tunnel) را انتخاب کنید.
 * نقش سرور را 2 (FOREIGN Server) انتخاب کنید.
 * Remote IP: آی‌پی سرور ایران را وارد کنید.
 * Internal IP: دقیقاً همان رنجی که در سرور ایران زدید (مثلاً 10.0.0) را وارد کنید.
🚀 قابلیت‌های کلیدی و نحوه استفاده
🛡️ عبور از فیلترینگ با تغییر پروتکل (گزینه ۳)
اگر احساس کردید سرعت تانل کم شده یا پکت‌لاس دارید، از گزینه Switch Protocol استفاده کنید.
 * GRE: سرعت عالی (مناسب گیمینگ).
 * IPIP: مخفی‌تر از GRE.
 * SIT: بهترین گزینه برای عبور از محدودیت‌های شدید (IPv6 over IPv4).
🐕 نگهبان هوشمند (گزینه ۵)
با فعال کردن Watchdog، اسکریپت هر ۲ دقیقه سلامت اتصال را چک می‌کند. اگر فایروال سعی در ایجاد اختلال داشته باشد، اسکریپت به صورت خودکار با تغییر پارامترهای MTU و MSS، تانل را ترمیم می‌کند.
📊 مانیتورینگ زنده ترافیک (گزینه ۴)
می‌توانید به صورت لحظه‌ای ببینید چقدر ترافیک بین دو سرور شما در حال رد و بدل شدن است.
⚡ بهینه‌سازی BBR (گزینه ۵ در منوی اصلی)
برای کاهش افت سرعت در مسیرهای بین‌المللی، حتماً BBR را فعال کنید تا پروتکل TCP سرور شما هوشمندتر عمل کند.
❓ سوالات متداول
 * آیا ترافیک من رمزنگاری می‌شود؟ خیر، پروتکل‌های GRE/IPIP رمزنگاری ندارند و فقط ترافیک را تونل می‌کنند. برای امنیت، سرویس‌های خود (مثل V2Ray) را روی این تانل سوار کنید.
 * چرا تانل وصل نمی‌شود؟ مطمئن شوید که فایروال هر دو سرور اجازه عبور پروتکل‌های ۴۷ (GRE) و ۴ (IPIP) را می‌دهد.
مایل هستید یک ویدیو یا گیف کوتاه از محیط برنامه برای اضافه کردن به آموزش تهیه کنیم؟
