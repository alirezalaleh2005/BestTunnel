# 🛡️ BestTunnel Ultimate Edition
**سیستم هوشمند تانلینگ GRE/IPIP/SIT برای سرورهای لینوکس**

این پروژه یک راهکار حرفه‌ای و سبک برای برقراری ارتباط پایدار و با سرعت بالا بین سرورهای ایران و خارج است که از پروتکل‌های بومی هسته لینوکس استفاده می‌کند.

---

## 🚀 نصب سریع (One-Liner Install)
برای نصب و اجرای مستقیم اسکریپت، دستور زیر را در ترمینال سرور خود کپی و اجرا کنید:

```bash
bash <(curl -Ls https://raw.githubusercontent.com/alirezalaleh2005/BestTunnel/main/install.sh)
```
نکته: پس از نصب، می‌توانید در هر زمان با تایپ دستور 
  ```bash    besttunnel ```
وارد منوی مدیریت شوید.
​✨ قابلیت‌های برجسته
​پشتیبانی از پروتکل‌های متنوع: امکان سوئیچ لحظه‌ای بین GRE, IPIP و SIT.
​سیستم پایداری هوشمند (Watchdog): بررسی خودکار سلامت تانل و رفع پکت‌لاس با تغییر داینامیک MTU/MSS.
​بهینه‌سازی BBR: فعال‌سازی الگوریتم کنترل ازدحام گوگل برای افزایش چشمگیر سرعت.
​آنتی DPI: تنظیمات پیشرفته برای دور زدن سیستم‌های بازرسی عمیق بسته‌ها.
​مانیتورینگ زنده: مشاهده لحظه‌ای ترافیک مصرفی روی اینترفیس تانل.
​Persistence: اجرای خودکار تانل پس از هر بار ریبوت سرور با استفاده از Systemd.
​📖 راهنمای استفاده
​در سرور ایران: اسکریپت را اجرا کرده، نقش را IRAN انتخاب کنید و آی‌پی سرور خارج را وارد کنید.
​در سرور خارج: اسکریپت را اجرا کرده، نقش را FOREIGN انتخاب کنید و آی‌پی سرور ایران را وارد کنید.
​مسیریابی: از گزینه Route Ports برای هدایت ترافیک پورت‌های مورد نظر (مثل پورت‌های پنل V2Ray) به داخل تانل استفاده کنید.
​🛠️ ساختار پروژه
​besttunnel.sh: هسته اصلی اسکریپت و منطق مدیریت شبکه.
​install.sh: فایل نصب‌کننده و ایجاد میانبر سیستم.
​besttunnel.conf: ذخیره تنظیمات برای پایداری پس از ریبوت.
​👨‍💻 توسعه‌دهنده
